"""BCFuzzer fuzzing engine built on the interaction-coverage-runner infrastructure.

Implements the components the upstream prototype lacks: the mutation-effective
index (MEI), the two-level type-aware scheduler, the T/M corpora, the BCB
oracle and the campaign driver — see BCFuzzer_refined_design.md §3.
"""
